import { LineChart } from "react-native-chart-kit";
import { StyleSheet, Text, Dimensions, View } from "react-native";

export default function Diagram() {
    const styles = StyleSheet.create({});

    return <Text>Diagram</Text>;
}
